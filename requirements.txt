# dashboard4dx — runtime dependencies (Python 3.11.x)
# All versions are pinned. The app makes no outbound network calls beyond
# `pip install -r requirements.txt`.

# UI framework
streamlit==1.39.0
streamlit-calendar==1.3.1

# Data wrangling
pandas==2.2.3
numpy==2.1.3
openpyxl==3.1.5

# Charts
plotly==5.24.1
matplotlib==3.9.2         # pure-Python PNG rendering for the PDF report (kaleido
                          # was replaced because its bundled Chromium failed to
                          # launch on locked-down Windows boxes)

# PDF report generation (uses built-in Heisei CJK font — no extra font file needed)
reportlab==4.2.5
